export const filterOutShorts = () => {
  // const richElems = document.querySelectorAll('ytd-rich-section-renderer ytd-rich-shelf-renderer');
  // // const reelElems = document.querySelectorAll('ytd-reel-shelf-renderer');

  // if(richElems.length > 0) {
  //   const shortsElem = Array.from(richElems).filter(el => el.hasAttribute('is-shorts'));

  //   for(let i = 0; i < shortsElem.length; i++) {
  //     shortsElem[i].parentNode.parentNode.remove();
  //   }
  // } else {
  //   setTimeout(filterOutShorts, 1000);
  // }    
};

export const filterOutShortsFromRecomendations = () => {
  // const reelElems = document.querySelectorAll('ytd-reel-shelf-renderer');

  // if(reelElems) {
  //     reelElems.remove();
  // } else {
  //   setTimeout(filterOutShortsFromRecomendations, 1000);
  // }    
};